import {Sdk} from "../sdk/sdk";

export const loadMintAndList = (sdk: Sdk) => {
    //prompt for a image 
    //upload image to IPFS
    //create metadata
    //mint
    //list
    //render view listing view --- or home menu
}